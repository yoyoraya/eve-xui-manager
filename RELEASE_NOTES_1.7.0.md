```
🚀 انتشار نسخه v1.7.0 — 2026-01-02

✨ امکانات جدید
- 🧑‍💼  مدیریت مالکیت: امکان اختصاص/لغو دسته‌ای مالک (Reseller) برای کلاینت‌ها.
- 🪪  استحکام مالکیت: کلید زدن مالکیت بر پایه `UUID` (اولویت) و سپس ایمیل — تغییر ایمیل مالک را خراب نمی‌کند.
- 🧭  ستون Owner: نمایش مالک در ستون مجزا در پنل مدیریت.
- ⚡  بهبود عملکرد رفرش: حذف الگوی N+1 در `/api/refresh`؛ نمایش آنلاین سریع‌تر با تشخیص زمان اجرای اختیاری (`debug_timing=1`).
- 🛠  نصب/آپدیت: رفع گیر کردن گزینه به‌روزرسانی (`git fetch`) با زمان‌سنج و پیام‌های عیب‌یابی.

🐞 رفع باگ‌ها
- 🔁  رفع مشکل تمدید: گذاشتن فیلد حجم خالی دیگر باعث ریست/تغییر ظرفیت نمی‌شود؛ وقتی حجم اضافه می‌شود متن تمدید مقدار «باقیمانده قبلی + اضافه‌شده» را نشان می‌دهد.
- 🧾  نسخه پنل به `1.7.0` ارتقا یافت.

🧩 یادداشت‌ها
- در صورت نیاز، مایگریشن‌های دیتابیس را اجرا کنید.
- اسکن امنیتی Snyk چند مورد (XSS/SSRF/SSL bypass) گزارش کرده — پیشنهاد می‌شود در یک PR جداگانه سختنینگ انجام شود.
```
