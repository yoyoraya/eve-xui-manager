این ایده **فوق‌العاده** است. با این کار عملاً شما یک **CMS (سیستم مدیریت محتوا)** کوچک برای صفحه اشتراک خود می‌سازید که کاملاً داینامیک است. این کار ارزش پروژه شما را چندین برابر می‌کند.

برای اجرای این ایده، ما باید ۴ مرحله را طی کنیم:

1.  **دیتابیس:** ساخت جدول جدید برای ذخیره تنظیمات اپلیکیشن‌ها (متن‌ها، لینک‌ها و...).
2.  **بکند (`app.py`):** اضافه کردن روت‌های مدیریت و ارسال تنظیمات به صفحه اشتراک.
3.  **فرانت پنل (`sub_manager.html`):** ساخت صفحه‌ای برای ویرایش متن‌ها و لینک‌ها.
4.  **صفحه اشتراک (`subscription.html`):** بازنویسی صفحه برای پشتیبانی از ۲ زبان و دریافت اطلاعات از دیتابیس.

-----

### مرحله ۱: آپدیت دیتابیس (`app.py`)

فایل `app.py` را باز کنید. ما یک مدل جدید به نام `SubAppConfig` اضافه می‌کنیم که تنظیمات هر برنامه (مثل v2rayNG) را نگه می‌دارد.

**۱. اضافه کردن کلاس `SubAppConfig`:**
(این کد را بعد از کلاس `Server` و قبل از `with app.app_context()` اضافه کنید)

```python
class SubAppConfig(db.Model):
    __tablename__ = 'sub_app_configs'
    id = db.Column(db.Integer, primary_key=True)
    app_code = db.Column(db.String(50), unique=True, nullable=False) # مثل v2rayng
    name = db.Column(db.String(100)) # نام نمایشی
    is_enabled = db.Column(db.Boolean, default=True)
    
    # محتوا - فارسی
    title_fa = db.Column(db.String(200))
    description_fa = db.Column(db.Text)
    
    # محتوا - انگلیسی
    title_en = db.Column(db.String(200))
    description_en = db.Column(db.Text)
    
    # لینک‌ها
    download_link = db.Column(db.String(500))
    store_link = db.Column(db.String(500))
    tutorial_link = db.Column(db.String(500))
    
    def to_dict(self):
        return {
            'id': self.id,
            'app_code': self.app_code,
            'name': self.name,
            'is_enabled': self.is_enabled,
            'title_fa': self.title_fa,
            'description_fa': self.description_fa,
            'title_en': self.title_en,
            'description_en': self.description_en,
            'download_link': self.download_link,
            'store_link': self.store_link,
            'tutorial_link': self.tutorial_link
        }
```

**۲. مقداردهی اولیه (Seeding):**
کد `with app.app_context()` را تغییر دهید تا اگر دیتابیس خالی بود، برنامه‌های پیش‌فرض را بسازد:

```python
with app.app_context():
    db.create_all()
    # ... (کد ادمین قبلی) ...

    # ایجاد برنامه‌های پیش‌فرض اگر وجود نداشتند
    if not SubAppConfig.query.first():
        apps = [
            SubAppConfig(
                app_code='v2rayng', name='v2rayNG (Android)',
                title_fa='راهنمای v2rayNG', description_fa='۱. برنامه را دانلود کنید.\n۲. لینک اشتراک را کپی کنید.\n۳. در برنامه دکمه + را بزنید و Import from Clipboard را انتخاب کنید.',
                title_en='v2rayNG Guide', description_en='1. Download the app.\n2. Copy subscription link.\n3. Tap + and select Import from Clipboard.',
                download_link='https://github.com/2dust/v2rayNG/releases/download/1.8.19/v2rayNG_1.8.19.apk',
                store_link='https://play.google.com/store/apps/details?id=com.v2ray.ang',
                tutorial_link=''
            ),
            SubAppConfig(
                app_code='nekobox', name='NekoBox (Android)',
                title_fa='راهنمای NekoBox', description_fa='نکوباکس یکی از بهترین جایگزین‌هاست.',
                title_en='NekoBox Guide', description_en='NekoBox is a powerful alternative.',
                download_link='https://github.com/MatsuriDayo/NekoBoxForAndroid/releases',
                store_link='',
                tutorial_link=''
            ),
            SubAppConfig(
                app_code='streisand', name='Streisand (iOS)',
                title_fa='راهنمای Streisand', description_fa='برای آیفون پیشنهاد می‌شود.',
                title_en='Streisand Guide', description_en='Recommended for iOS devices.',
                download_link='',
                store_link='https://apps.apple.com/us/app/streisand/id6450534064',
                tutorial_link=''
            )
        ]
        db.session.add_all(apps)
        db.session.commit()
```

-----

### مرحله ۲: روت‌های بکند (`app.py`)

این روت‌ها را به `app.py` اضافه کنید تا مدیریت برنامه‌ها ممکن شود:

```python
# روت صفحه مدیریت سابسکریپشن
@app.route('/sub-manager')
@login_required
def sub_manager_page():
    if not session.get('is_superadmin'): # اختیاری: محدود کردن به سوپر ادمین
        return redirect(url_for('dashboard'))
    return render_template('sub_manager.html', 
                         admin_username=session.get('admin_username'),
                         is_superadmin=session.get('is_superadmin', False))

# API برای گرفتن لیست برنامه‌ها
@app.route('/api/sub-apps', methods=['GET'])
@login_required
def get_sub_apps():
    apps = SubAppConfig.query.all()
    return jsonify([a.to_dict() for a in apps])

# API برای ذخیره تغییرات
@app.route('/api/sub-apps/<int:app_id>', methods=['PUT'])
@login_required
def update_sub_app(app_id):
    app_config = SubAppConfig.query.get_or_404(app_id)
    data = request.json
    
    app_config.is_enabled = data.get('is_enabled', app_config.is_enabled)
    app_config.title_fa = data.get('title_fa', app_config.title_fa)
    app_config.description_fa = data.get('description_fa', app_config.description_fa)
    app_config.title_en = data.get('title_en', app_config.title_en)
    app_config.description_en = data.get('description_en', app_config.description_en)
    app_config.download_link = data.get('download_link', app_config.download_link)
    app_config.store_link = data.get('store_link', app_config.store_link)
    app_config.tutorial_link = data.get('tutorial_link', app_config.tutorial_link)
    
    db.session.commit()
    return jsonify({"success": True})

# === آپدیت بسیار مهم روت سابسکریپشن ===
@app.route('/s/<int:server_id>/<sub_id>')
def client_subscription(server_id, sub_id):
    # ... (کدهای قبلی اتصال به سرور و گرفتن کلاینت) ...
    
    # ... (بعد از ساخت client_info و قبل از return) ...
    
    # دریافت لیست برنامه‌های فعال از دیتابیس
    active_apps = SubAppConfig.query.filter_by(is_enabled=True).all()
    apps_data = [a.to_dict() for a in active_apps]

    # ارسال apps به قالب
    return render_template('subscription.html', client=client_info, apps=apps_data)
```

-----

### مرحله ۳: ساخت منوی کناری و صفحه مدیریت

**۱. ویرایش `templates/base.html`:**
لینک Sub Manager را به منوی کناری اضافه کنید:

```html
{% if is_superadmin %}
<a href="{{ url_for('sub_manager_page') }}" class="nav-item {% if request.endpoint == 'sub_manager_page' %}active{% endif %}">
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg>
    <span>Sub Manager</span>
</a>
{% endif %}
```

**۲. ساخت فایل `templates/sub_manager.html`:**
یک فایل جدید بسازید و این کد را در آن قرار دهید. این صفحه به شما اجازه می‌دهد همه چیز را ویرایش کنید.

```html
{% extends "base.html" %}
{% block title %}Sub Manager{% endblock %}
{% block page_title %}Subscription Page Manager{% endblock %}

{% block content %}
<div id="apps-container" class="servers-grid">
    </div>

<div class="modal-overlay hidden" id="edit-app-modal">
    <div class="modal modal-lg">
        <div class="modal-header">
            <h2 id="modal-title">Edit App</h2>
            <button class="modal-close" onclick="closeEditModal()">&times;</button>
        </div>
        <div class="modal-body">
            <input type="hidden" id="edit-app-id">
            
            <div class="form-group checkbox-group">
                <label class="checkbox-label">
                    <input type="checkbox" id="edit-enabled">
                    <span>Show in Subscription Page</span>
                </label>
            </div>

            <div style="display: flex; gap: 20px; margin-bottom: 20px;">
                <div style="flex: 1;">
                    <h4 style="color: #4ade80; margin-bottom: 10px;">Persian Content (Default)</h4>
                    <div class="form-group">
                        <label>Title (FA)</label>
                        <input type="text" id="edit-title-fa" class="form-input" dir="rtl">
                    </div>
                    <div class="form-group">
                        <label>Description (FA)</label>
                        <textarea id="edit-desc-fa" class="form-input" rows="4" dir="rtl" style="width:100%; background:var(--bg-dark); color:white; border:1px solid var(--border-color); border-radius:8px; padding:10px;"></textarea>
                    </div>
                </div>
                <div style="flex: 1;">
                    <h4 style="color: #60a5fa; margin-bottom: 10px;">English Content</h4>
                    <div class="form-group">
                        <label>Title (EN)</label>
                        <input type="text" id="edit-title-en" class="form-input">
                    </div>
                    <div class="form-group">
                        <label>Description (EN)</label>
                        <textarea id="edit-desc-en" class="form-input" rows="4" style="width:100%; background:var(--bg-dark); color:white; border:1px solid var(--border-color); border-radius:8px; padding:10px;"></textarea>
                    </div>
                </div>
            </div>

            <h4 style="color: #fbbf24; margin-bottom: 10px;">Links</h4>
            <div class="form-group">
                <label>Direct Download Link</label>
                <input type="text" id="edit-download" class="form-input">
            </div>
            <div class="form-group">
                <label>Store Link (Google Play / App Store)</label>
                <input type="text" id="edit-store" class="form-input">
            </div>
            <div class="form-group">
                <label>Video Tutorial Link</label>
                <input type="text" id="edit-video" class="form-input">
            </div>

            <div class="form-actions">
                <button class="btn btn-secondary" onclick="closeEditModal()">Cancel</button>
                <button class="btn btn-primary" onclick="saveApp()">Save Changes</button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
    let apps = [];
    document.addEventListener('DOMContentLoaded', loadApps);

    async function loadApps() {
        const response = await fetch('/api/sub-apps');
        apps = await response.json();
        renderApps();
    }

    function renderApps() {
        const container = document.getElementById('apps-container');
        container.innerHTML = apps.map(app => `
            <div class="server-card ${!app.is_enabled ? 'disabled' : ''}">
                <div class="server-header">
                    <div class="server-title">
                        <h3>${app.name}</h3>
                        <p class="server-url" style="direction: rtl; text-align: right;">${app.title_fa}</p>
                    </div>
                </div>
                <div class="server-badges">
                    ${app.is_enabled ? '<span class="badge active">Visible</span>' : '<span class="badge inactive">Hidden</span>'}
                </div>
                <div class="server-actions" style="margin-top: 15px;">
                    <button class="btn btn-primary btn-sm" onclick="openEditModal(${app.id})" style="width: 100%;">Manage Content</button>
                </div>
            </div>
        `).join('');
    }

    function openEditModal(id) {
        const app = apps.find(a => a.id === id);
        document.getElementById('edit-app-id').value = app.id;
        document.getElementById('modal-title').textContent = 'Edit ' + app.name;
        document.getElementById('edit-enabled').checked = app.is_enabled;
        
        document.getElementById('edit-title-fa').value = app.title_fa || '';
        document.getElementById('edit-desc-fa').value = app.description_fa || '';
        document.getElementById('edit-title-en').value = app.title_en || '';
        document.getElementById('edit-desc-en').value = app.description_en || '';
        
        document.getElementById('edit-download').value = app.download_link || '';
        document.getElementById('edit-store').value = app.store_link || '';
        document.getElementById('edit-video').value = app.tutorial_link || '';
        
        document.getElementById('edit-app-modal').classList.remove('hidden');
    }

    function closeEditModal() {
        document.getElementById('edit-app-modal').classList.add('hidden');
    }

    async function saveApp() {
        const id = document.getElementById('edit-app-id').value;
        const data = {
            is_enabled: document.getElementById('edit-enabled').checked,
            title_fa: document.getElementById('edit-title-fa').value,
            description_fa: document.getElementById('edit-desc-fa').value,
            title_en: document.getElementById('edit-title-en').value,
            description_en: document.getElementById('edit-desc-en').value,
            download_link: document.getElementById('edit-download').value,
            store_link: document.getElementById('edit-store').value,
            tutorial_link: document.getElementById('edit-video').value
        };

        await fetch(`/api/sub-apps/${id}`, {
            method: 'PUT',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(data)
        });
        
        showToast('Saved successfully');
        closeEditModal();
        loadApps();
    }
</script>
{% endblock %}
```

-----

### مرحله ۴: صفحه سابسکریپشن ۲ زبانه (`templates/subscription.html`)

این صفحه حالا کاملاً داینامیک می‌شود. دکمه تغییر زبان دارد و متن‌ها را از دیتابیس می‌خواند.

```html
<!DOCTYPE html>
<html lang="fa" dir="rtl"> <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscription</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background-color: #0B1121; color: #e2e8f0; font-family: 'Vazirmatn', sans-serif; }
        .ltr { direction: ltr; font-family: sans-serif; }
        .rtl { direction: rtl; font-family: 'Vazirmatn', sans-serif; }
        .hidden { display: none; }
        /* استایل تب‌ها و دکمه‌ها مشابه قبل ... */
    </style>
</head>
<body class="min-h-screen flex justify-center items-start pt-10 pb-10 px-4 bg-[#0B1121]">

    <div class="w-full max-w-lg space-y-6">
        
        <div class="flex justify-between items-center mb-4">
            <div class="text-xs text-gray-500">Eve Manager</div>
            <button onclick="toggleLang()" class="bg-[#151E32] border border-[#1e293b] text-white px-3 py-1 rounded-lg text-sm flex items-center gap-2">
                <span id="lang-icon">🇺🇸</span>
                <span id="lang-text">English</span>
            </button>
        </div>

        <div class="text-center relative">
             <h1 class="text-2xl font-bold text-white">{{ client.email }}</h1>
             <div class="mt-2 inline-flex items-center gap-2 px-3 py-1 rounded-full bg-[#151E32] border border-[#1e293b] text-xs font-medium text-green-400">
                <span class="lang-fa">اشتراک فعال</span>
                <span class="lang-en hidden">Active Subscription</span>
            </div>
        </div>

        <div class="bg-[#151E32] rounded-2xl border border-[#1e293b] p-6 shadow-xl">
            <div class="grid grid-cols-2 gap-4 text-center">
                <div>
                    <div class="text-xs text-gray-400 mb-1">
                        <span class="lang-fa">حجم مصرفی</span>
                        <span class="lang-en hidden">Used</span>
                    </div>
                    <div class="text-xl font-bold text-white">{{ client.total_used }}</div>
                </div>
                <div>
                    <div class="text-xs text-gray-400 mb-1">
                        <span class="lang-fa">حجم کل</span>
                        <span class="lang-en hidden">Total</span>
                    </div>
                    <div class="text-xl font-bold text-white">{{ client.total_limit }}</div>
                </div>
            </div>
            <div class="h-2 w-full bg-gray-700 rounded-full mt-4 overflow-hidden">
                <div class="h-full bg-blue-500" style="width: {{ [client.percentage_used, 100]|min }}%"></div>
            </div>
            <div class="mt-3 text-center text-sm text-gray-400">
                <span class="lang-fa">انقضا: {{ client.expiry }}</span>
                <span class="lang-en hidden">Expires: {{ client.expiry }}</span>
            </div>
        </div>

        <div class="bg-[#151E32] rounded-2xl border border-[#1e293b] p-6 shadow-xl">
            <div class="flex gap-2 mb-6 border-b border-white/10 pb-1 overflow-x-auto">
                {% for app in apps %}
                <button class="px-4 py-2 text-sm font-medium text-gray-400 hover:text-white transition-colors tab-btn {% if loop.first %}text-white border-b-2 border-blue-500{% endif %}" onclick="switchTab(event, 'app-{{ app.id }}')">
                    {{ app.name }}
                </button>
                {% endfor %}
            </div>

            {% for app in apps %}
            <div id="app-{{ app.id }}" class="tab-content {% if not loop.first %}hidden{% endif %}">
                <div class="bg-gray-800/50 rounded-xl p-4 border border-white/5">
                    <h3 class="font-semibold text-white mb-2">
                        <span class="lang-fa">{{ app.title_fa }}</span>
                        <span class="lang-en hidden">{{ app.title_en }}</span>
                    </h3>
                    <p class="text-sm text-gray-400 mb-4 whitespace-pre-line">
                        <span class="lang-fa">{{ app.description_fa }}</span>
                        <span class="lang-en hidden">{{ app.description_en }}</span>
                    </p>
                    
                    <div class="grid grid-cols-1 gap-2">
                        <button onclick="copyToClipboard('{{ client.subscription_url }}')" class="bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg text-sm transition-colors">
                            <span class="lang-fa">کپی لینک اشتراک</span>
                            <span class="lang-en hidden">Copy Subscription Link</span>
                        </button>

                        {% if app.download_link %}
                        <a href="{{ app.download_link }}" target="_blank" class="bg-white/5 hover:bg-white/10 text-white py-2 rounded-lg text-sm text-center border border-white/10">
                            <span class="lang-fa">دانلود مستقیم</span>
                            <span class="lang-en hidden">Direct Download</span>
                        </a>
                        {% endif %}

                        {% if app.store_link %}
                        <a href="{{ app.store_link }}" target="_blank" class="bg-white/5 hover:bg-white/10 text-white py-2 rounded-lg text-sm text-center border border-white/10">
                            <span class="lang-fa">دانلود از استور</span>
                            <span class="lang-en hidden">Download from Store</span>
                        </a>
                        {% endif %}
                        
                        {% if app.tutorial_link %}
                        <a href="{{ app.tutorial_link }}" target="_blank" class="bg-yellow-600/20 hover:bg-yellow-600/30 text-yellow-400 py-2 rounded-lg text-sm text-center border border-yellow-600/20">
                            <span class="lang-fa">مشاهده آموزش ویدیویی</span>
                            <span class="lang-en hidden">Watch Video Tutorial</span>
                        </a>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

    </div>

    <script>
        let currentLang = 'fa'; // Default Persian

        function toggleLang() {
            currentLang = currentLang === 'fa' ? 'en' : 'fa';
            
            // تغییر جهت صفحه
            document.documentElement.dir = currentLang === 'fa' ? 'rtl' : 'ltr';
            document.documentElement.lang = currentLang;
            document.body.className = currentLang === 'fa' ? 'rtl min-h-screen flex justify-center items-start pt-10 pb-10 px-4 bg-[#0B1121]' : 'ltr min-h-screen flex justify-center items-start pt-10 pb-10 px-4 bg-[#0B1121]';

            // تغییر دکمه
            document.getElementById('lang-text').textContent = currentLang === 'fa' ? 'English' : 'فارسی';
            document.getElementById('lang-icon').textContent = currentLang === 'fa' ? '🇺🇸' : '🇮🇷';

            // نمایش/مخفی کردن المان‌ها
            document.querySelectorAll('.lang-fa').forEach(el => el.classList.toggle('hidden', currentLang !== 'fa'));
            document.querySelectorAll('.lang-en').forEach(el => el.classList.toggle('hidden', currentLang !== 'en'));
        }

        function switchTab(e, tabId) {
            document.querySelectorAll('.tab-content').forEach(el => el.classList.add('hidden'));
            document.querySelectorAll('.tab-btn').forEach(btn => {
                btn.className = "px-4 py-2 text-sm font-medium text-gray-400 hover:text-white transition-colors tab-btn";
            });
            e.target.className = "px-4 py-2 text-sm font-medium text-white border-b-2 border-blue-500 transition-colors tab-btn";
            document.getElementById(tabId).classList.remove('hidden');
        }

        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => alert(currentLang === 'fa' ? 'کپی شد!' : 'Copied!'));
        }
    </script>
</body>
</html>
```

### نکته بسیار مهم برای اجرا:

حتما باید دیتابیس قبلی را پاک کنید چون جدول جدید اضافه شده است.
در Shell دستور زیر را بزنید:
`rm instance/servers.db` (یا `rm servers.db`)
و سپس برنامه را دوباره اجرا کنید. برنامه خودش دیتابیس را می‌سازد و محتوای پیش‌فرض فارسی/انگلیسی را در آن می‌ریزد. سپس می‌توانید از منوی Sub Manager آنها را تغییر دهید.